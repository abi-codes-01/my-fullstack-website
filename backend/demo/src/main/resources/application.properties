spring.application.name=demo
# Run the backend server on port 8080
server.port=8080

# Connect to MySQL and automatically create the 'auth_db' database if it doesn't exist
spring.datasource.url=jdbc:mysql://localhost:3306/auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=silambarasan

# Automatically manage database tables based on Java code changes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true